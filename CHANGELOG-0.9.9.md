# DropAgentX — Changelog 0.9.9 «روژن»

تاریخ: ۲۰۲۶-۰۸-۲۹ · آستانهٔ ۱.۰ — مغازه حالا چشم تحلیلی دارد 👁

## 🆕 موتور تحلیل رشد (روژن)
- **داشبورد `/insights`** (ادمین): نمودار SVG خالص ۱۴روزه — کاربر جدید/فعال روزانه،
  گردش خرید (GMV)، KPIها (کل کاربران، فعال ۷روز، جدید ۷روز، فروش، AOV)،
  پرفروش‌ترین محصولات و توزیع دسته‌ها — بدون هیچ کتابخانهٔ بیرونی، آفلاین کار می‌کند
- `GET /api/admin/insights` — دادهٔ تحلیل (محافظت ادمین)
- دکمهٔ «📈 تحلیل روژن» در نوار پنل ادمین · رفرش خودکار ۶۰ثانیه

## 🆕 خروجی CSV (برای اکسل — با BOM فارسی)
`/api/admin/export/{users|products|sales}.csv`

## 🆕 هفته‌نامهٔ فروشندگان
شنبه‌ها برترین فروشندگان ۷روز + جمع‌بندی، خودکار به چت ادمین‌ها
(تنظیم‌شونده با setting «last_weekly_digest»، منطق تست‌شده)

## 🆕 پک مهارت هرمس → ۱۶ مهارت
۴ مهارت جدید: نگه‌داشت مشتری · اپ‌سل/کراس‌سل · تبلیغات تلگرام · سواد تحلیل

## 🐛 باگ نهفته فیکس‌شده
- **`purchases.created_at` هرگز ساخته نشده بود** ولی لیدربورد خریدارها به آن
  ارجاع می‌داد → ۵۰۰ تای پنهان. مهاجرت + بک‌فیل از `purchased_at` اضافه شد.

## ✅ QA
**۹۳/۹۳ تست** (+۶ تست روژن: ریاضی موتور تحلیل با دادهٔ کنترل‌شده، هفته‌نامه،
گارد صفحه، CSV، پک ۱۶مهارت) · کامپایل کل ✓ · healthz = 0.9.9 ✓
