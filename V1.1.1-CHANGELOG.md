# 🛡 DropAgentX v1.1.1 — نسخهی پایدار، بدون باگ

> مبنا: **v1.1.0** (`DropAgentXmain`). همان پایهای که الان روی Railway داری + فیکسهای زیر.
> هدف: نسخهای که بتوانی با خیال راحت روی Railway نگه داری، **بدون کرشِ `/start`** و بدون ازدسترفتن حافظه.

---

## ✅ باگهای برطرفشده (۷ باگ)

### باگ ۱ (بحرانی) — کرش خوشآمدگویی `/start` ✅
- **علت:** `handlers/start.py` با `parse_mode="Markdown"` (Markdown V1) ارسال میکرد.
  متن فارسی RTL + ایموجی + اعداد، در Markdown V1 بهخاطر شمارش offset بایتی دچار
  `TelegramBadRequest: can't parse entities... byte offset 29` میشد → کاربرِ تازهوارد
  **هیچ پیامی نمیگرفت** (نقطهی ورودِ کل سفر او خاموش بود).
- **رفع:** کل مسیر ارسال حالا از `send_safe`/`edit_safe` میگذرد که **MarkdownV2** را با
  escape درست برای فارسی/ایموجی/اعداد مدیریت میکند، و در صورت شکست به متن ساده برمیگردد.
  پیشفرضِ bot-wide `parse_mode` هم به `None` تغییر کرد تا هیچ پیامی بهطور ناخواسته کرش نکند.

### باگ ۲ — نشت state دیتابیس بین تستها ✅
- **علت:** singleton دیتابیس و کشِ `_USER_CACHE` فقط به `user_id` کی میشد، نه مسیر DB.
  وقتی دیتابیس ایزوله عوض میشد، `user_id` تکراری از تست قبل در کش میماند →
  `FOREIGN KEY constraint failed` و شکستهای فلکی.
- **رفع:** singleton **مسیر-آگاه** شد و کشِ کاربر **مسیر-آگاه** شد.

### باگ ۳ — `audit_history` همیشه خالی ✅
- **علت:** در `approve/reject_request` سطر audit با `user_id` **مدیرِ تأییدکننده** ثبت میشد،
  نه کاربرِ درخواستدهنده.
- **رفع:** `user_id` حالا درخواستدهنده است، ستون `actor_id` برای عامل تصمیم اضافه شد.

### باگ ۴ — sandbox escape در `file_tools.py` ✅
- **علت:** `PROJECT_ROOT` موقع import فریز میشد.
- **رفع:** تابع `_root()` که `FILE_TOOLS_ROOT` را هنگام هر فراخوانی میخواند.

### باگ ۵ — مهاجرتهای ایمن ✅
- `ALTER TABLE ... ADD COLUMN` هر کدام در `try/except` با فالبک، تا روی دیتابیس موجود
  (که ستونها دارند) **crash نکند**.

### باگ ۶ — ایمنی ارسال Markdown در ۱۷ نقطهی دیگر ✅
- تمام `parse_mode="Markdown"` خام در `admin/ai_chat/help/products/org` به `send_safe`
  تبدیل شدند تا همان کرش RTL در هیچ منویی تکرار نشود.

### باگ ۷ — لاگهای بیصدا ✅
- چند هندلر exception بیصدا لاگگرفتنمیکردند؛ حالا لاگ میشوند.

---

## ✅ چرا حافظهی ربات سالم میماند؟

- **مسیر دیتابیس همان است:** `DB_PATH = data/marketplace.db`.
- **همهی جدولها ستونبهستون یکساناند** (مقایسهی واقعی انجام شد):
  `users, purchases, products, transactions, withdrawals, deposits, tasks,
  user_memories, user_profile, follows, settings, custom_bots, coupons, reviews,
  product_comments, product_engagement, hermes_sessions, ref_milestones,
  role_audit, hunter_permissions, cron_tasks, ad_campaigns, content_pages, kb_notes`.
- **هیچ ستونی حذف/تغییرنام نشده؛ فقط اضافه میشود** و هر ADD با `try/except` ایمن است.
- **حافظه روی Volume ریلكِی است، نه در git** → `git push` دادهها را پاک نمیکند.

---

## 🧪 تأیید کیفیت
```
pytest                →  47 passed   (43 پایه + ۴ تست ایمنی MarkdownV2)
python -m compileall  →  OK
import bot            →  BOT_IMPORT_OK
دیتابیس + حافظه        →  user/receیخ/بازیابی حافظه ✔
```

## 📊 آمار
- پروژه: 5,786 خط Python (پایه) | 43+4 تست
- **صفر** `parse_mode="Markdown"` خام باقیمانده در handlers.

---

## 🚀 استقرار امن (خلاصه)
1. **بکاپ** از `data/marketplace.db` (پنل ادمین → Backup، یا دانلود فایل).
2. مطمئن شو **Railway Volume** روی `data` (و `uploads`) mount است.
3. کد v1.1.1 را push کن → `docker build` → `up -d --build`.
4. **`docker compose down -v` را اجرا نکن** (`-v` = حذف Volume = از بین رفتن حافظه).
5. بعد از بالا آمدن چک کن: `/system` (کاربران/خرید حفظ شده) و `pytest` محلی.

## 📌 فایلهای تغییر یافته
`config.py` (v1.1.1), `bot.py` (default parse_mode=None + observability),
`database.py` (singleton/cache مسیر-آگاه), `file_tools.py` (root داینامیک),
`approval.py` (actor_id + audit), `sync/cleanup`, `utils.py` (MarkdownV2 safe),
`handlers/*` (send_safe در ۱۷ نقطه), `tests/test_core.py` (+۴ تست).
