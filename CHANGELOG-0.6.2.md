# DropAgentX — Changelog 0.6.2 «دیپلوی پایدار»

تاریخ: ۲۰۲۶-۰۸-۲۹ · برای هنگ‌کردن بیلد Railway («Building the image» بی‌پایان)

## 🔧 تغییرات بیلد
1. **پایهٔ ایمیج → `mirror.gcr.io/library/python:3.12-slim`:** عامل شایع هنگِ بیلد،
   Docker Hub pull-rate-limit است (مخصوصاً با چند دپلوی پشت‌سرهم) — میرور GCR
   این محدودیت را دور می‌زند.
2. **`PIP_DEFAULT_TIMEOUT=100` + `PIP_RETRIES=5`:** لرزش شبکه دیگر pip را
   بی‌نهایت معطل نمی‌کند.
3. راهنمای عیب‌یابی بیلد به `docs/DEPLOY-VPS-RAILWAY-FA.md` اضافه شد
   (لغو دپلوی‌های تکراری، خواندن لاگ بیلد مرحله‌به‌مرحله، دامنه 8080).

## ✅ بدون تغییر رفتاری
کد اپلیکیشن دست نخورده — همان 0.6.1 با ۸۷/۸۷ تست.
