# CHANGELOG — DropAgentX v1.2.1 (Launch Release)

تاریخ: ۲۰۲۶-۰۸-۳۰ · نوع: minor + patch (لانچ نهایی)

## ✨ جدید (Instagram-algorithm layer)
- **فید شخصی‌سازی‌شده (For You):** رنکینگ سه‌لایه — سیگنال‌های رفتاری واقعی
  (خرید ×۵، سیو ×۴، لایک ×۳، کامنت ×۲) + decay زمانی ۲روزه،
  امتیاز علاقه‌مندی دسته (affinity) از تاریخچه‌ی خرید (۳۵+) و لایک/سیو (۲۲+)،
  بوست ۴۵+ برای سازندگانِ فالو‌شده، و jitter تصادفی برای تنوع فید.
- **Share-to-DM:** اشتراک‌گذاری محصول داخل تلگرام با انتخاب چت
  (t.me/share/url) و پیام پیش‌پرشده — حلقه‌ی رشد کاربر-به-کاربر.

## 📱 بهبود موبایل (از v1.1)
- pull-to-refresh، اسپلش بوت، متاهای PWA، کیبورد-aware چت هرمسا،
  چیپ‌های پیشنهاد، تاریخچه‌ی چت، حذف تأخیر ۳۰۰ms لمس، اعداد tabular.

## 🔐 رابط نقش‌محور (از v1.1)
- `is_admin` از سرور → دکمه‌ی 🛡 فقط برای ادمین؛ بج نقش/هانتر؛
  فیلتر لینک‌های مدیریتی از هاب برای کاربر عادی.

## 🐛 فیکس‌ها
- `await` جاافتاده‌ی `rl_signal` (سیستم Identity RL اکنون واقعاً کار می‌کند)
- دکمه‌های مرده‌ی کارت‌ها در نتایج جستجو (DGX.wireFeed مشترک)
- نقطه‌ی اعلان فیک همیشگی → اعلان واقعی مبتنی بر خواندن
- شمارنده‌ی لایک پس از تعامل
- (v1.0.1) روتینگ skills/hunters پنل ادمین، لینک دانلود امضادار،
  آواتار عمومی، لوگوی بازگردانده، پنل کامل مدیریت کاربران

## 🧪 تست‌شده قبل از لانچ
سینتکس تمام فایل‌های JS با QuickJS ✅ · SQL فید شخصی‌سازی‌شده روی SQLite ✅ ·
اسموک‌تست سرور (healthz/feed/assets/admin) ✅ · رگرسیون v1.0.1 ✅


## 🎨 سیستم آیکون SVG (همان نسخه — قبل از پوش نهایی)

- **حذف کامل اموجی از UI** — همه‌ی آیکون‌های رابط به SVG اینلاین (sprite با ۵۰ `<symbol>` در index.html) تبدیل شدند.
- `DGX.icon(name, cls)` در ui.js — رندر `<svg><use href="#i-…">`؛ رنگ با `currentColor` از تم پیروی می‌کند.
- `DGX.CAT_ICONS` + `DGX.catIcon(key)` — مپ دسته→آیکون در کلاینت؛ سرور دست‌نخورده (پس‌سازگار، صفر ریسک API).
- پوشش: هاب، استوری‌ها، کارت محصول، خرید، کیف پول (تراکنش‌ها)، پروفایل (بج ادمین/هانتر)، جست‌وجو، اکسپلور، ویزارد ساخت (۴ مرحله)، چت هرمسا، تب‌های فید/اکتیویتی.
- ۳۴ آیکون جدید (cart, rocket, wallet, flame, bot, shield, target, …) + CSS (`svg.ic` / `.ic-s` / `.cat-ic` / `.big-ic`).
- اموجی فقط در متن‌های محاوره‌ای (توست/اشتراک‌گذاری/پیام بات) مانده — مثل اینستاگرام.
