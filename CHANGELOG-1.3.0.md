# DropAgentX — v1.3.0 «NEON»

> رتم کامل با DNA بصری 0xBazaar، روی استخوان‌بندی DropAgentX — بدون تغییر API.

## 🎨 رتم NEON (همه‌ی اپ)
- پالت جدید: پس‌زمینه‌ی نئون‌بلک `#050505`، سطوح چارکوال `#0B0F0D`، خطوط خنثی (حذف کامل ته‌رنگ سرمه‌ای/آبی).
- سبز نئون `#00FF88` = اکست اصلی؛ فیروزه‌ای `#39d7ff` فقط glow ثانویه؛ طلایی فقط میکرو-اکست.
- تاپ‌بار و نویگیشن پایین: شیشه‌ای چارکوال + هاله‌ی سبز.
- رینگ استوری‌ها و آواتارها: گرادیان سبز نئون (به‌جای رنگین‌کمان آبی-بنفش).
- لوگوی تاپ‌بار: کینگ سبز + تیترال گرادیان سبز→فیروزه‌ای.
- فوکوس اینپوت‌ها، چیپ‌ها، تب‌ها، دکمه‌ی اصلی: همگی با glow سبز هماهنگ.

## 🛒 جریان خرید جدید (ایده‌ی 0xBazaar 06/07/33)
- **شییت تأیید خرید** (`DGX.confirmBuy`): خلاصه‌ی محصول + قیمت + کارمزد خریدار (۰) + موجودی کیف پول + جمع پرداخت — قبل از هر خرید، در فید و صفحه‌ی محصول.
- هشدار «موجودی کافی نیست» داخل شییت + دکمه‌ی غیرفعال.
- **اُوَرلی موفقیت خرید** (`DGX.purchaseSuccess`): پالس سبز + تیک انفجاری + کارت محصول + شناسه‌ی تراکنش کوتاه + «دانلود محصول» + «سفارش‌های من» + «اشتراک‌گذاری خرید» (Share-to-DM).
- API دست‌نخورده: همان `POST /api/app/buy/{id}`.

## 🔧 زیرساخت
- `DGX.sheet()` / `DGX.closeSheet()` — کامپوننت پایه‌ی bottom-sheet قابل استفاده‌ی مجدد (فیلترها، شیر و…).
- CSS: لایه‌ی `NEON LAYER` انتهای design-system.css (بدون دست‌کاری قواعد قدیمی — override امن).
- VERSION = 1.3.0

## ✅ رگرسیون
- فید پرسونالایز v1.2.1، Share-to-DM، سیستم آیکون SVG (۵۰ سیمبل)، اسکلتون‌ها — همه دست‌نخورده و تست‌شده.
