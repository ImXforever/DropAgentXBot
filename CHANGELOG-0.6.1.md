# DropAgentX — Changelog 0.6.1 «هات‌فیکس لاگ»

تاریخ: ۲۰۲۶-۰۸-۲۹ · از روی لاگ واقعی Railway دیباگ شد

## 🐛 فیکس‌شده (از لاگ تولید)
1. **NameError: time — صفحهٔ «کاربران» ادمین کرش می‌کرد**
   `handlers/admin.py` از `time.time()` استفاده می‌کرد بدون `import time`
   (خطوط ۲۱۸/۲۹۹/۳۹۵) → import اضافه شد + تست اسکنر جدید که کل پروژه را برای
   «ماژول استفاده‌شده ولی import‌نشده» می‌گردد تا دیگر تکرار نشود.
2. **TelegramBadRequest: MESSAGE_TOO_LONG در دکمه‌های راهنمای کمپین**
   `callback.answer()` سقف ۲۰۰ کاراکتر دارد؛ متن‌های راهنما بلندتر بودند →
   متن بلند حالا پیام کامل ارسال می‌شود و alert خلاصه می‌دهد.

## ⚙️ کانفیگ (خودت باید عوض کنی — باگ کد نبود)
- **AI 404:** مدل `stealth/ox-alpha` بازنشسته شده (دورهٔ تست تمام شد).
  در Railway: `AI_MODEL` → مدل زنده (مثلاً `z-ai/glm-5.3-flash`) یا مثل لاگ آخرت
  که خودت سوییچ کردی به روتر شخصی (۲۰۰ OK ✓) همان بماند.

## ✅ QA
**۸۷/۸۷ تست** (+۲ رگرسیون هات‌فیکس) · کامپایل کل ✓ · healthz = 0.6.1 ✓
